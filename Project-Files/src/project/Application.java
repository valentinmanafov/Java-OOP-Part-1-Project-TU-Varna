package project;

/**
 * The main entry point for the Database CLI.
 */
public class Application {
    /**
     * The main method that starts the command line interface.
     */
    public static void main(String[] args) {
        CLI cli = new CLI();
        cli.start();
    }
}