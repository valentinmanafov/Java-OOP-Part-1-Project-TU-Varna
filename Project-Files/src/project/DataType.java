package project;

/**
 * Enumerates the supported data types for table columns.
 */
public enum DataType {
    /** Represents an integer data type. */
    INTEGER,
    /** Represents a double data type. */
    DOUBLE,
    /** Represents a string data type. */
    STRING
}